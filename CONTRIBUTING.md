# Contributing

RoDots is still small and experimental. Contributions are welcome when they keep the core idea simple: practical documents that help non-technical teams work with less friction.

Before opening a larger pull request, prefer starting with an issue that describes the workflow being improved.

## Local Checks

```bash
npm run check
npm run build
```

Rust/Tauri checks should be run on a machine with Rust/Cargo installed.
