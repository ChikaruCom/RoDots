# RoDots extensions

Put shared extension files for the portable distribution here.

This folder is intentionally bundled beside `rodots.exe` so a team can keep common behavior in the shared Dropbox folder while RoDots stores runtime caches locally on each PC.
